<!DOCTYPE html>
<html>
  <head>
    <title>{{appName}}</title>
    <meta name="viewport" content="initial-scale=1.0, minimum-scale=1.0">
    <style>

      html {
        box-sizing: border-box;
      }

      *, *:before, *:after {
        box-sizing: inherit;
      }

      html, body {
        height: 100%;
        margin: 0;
      }

      body {
        font-size: 1.1em;
      }

      ul {
        padding-left: 2em;
      }

      ul li {
        margin: 0.2em 0;
      }

      .main {
        position: relative;
        overflow: hidden;
        min-height: 100%;
        padding-bottom: 6em;
      }

      body {
        font-family: Helvetica;
      }

      a {
        color: inherit;
      }

      .inner {
        margin: 0 auto;
        padding: 0 0.8em;
        max-width: 940px;
      }

      footer {
        height: 4em;
        line-height: 4em;
        text-align: center;
        position: absolute;
        bottom: 0;
        width: 100%;
      }

      header {
        overflow: hidden;
        text-align: center;
        position: relative;
        overflow: hidden;
      }

      header .inner {
        height: 180px;
      }

      header .hero {
        position: absolute;
        top: 50%;
        left: 50%;
        transform: translate(-50%, -50%);
        width: 100%;
        padding: 0 1.4em;
      }

      h2 {
        font-size: 1.4em;
      }

      header h1 {
        margin: 0;
        margin-bottom: 0.2em;
        font-size: 2.2em;
        overflow: visible;
        position: relative;
        display: inline-block;
      }

      body {
        font-weight: 100;
      }

      h1, h2, h3 {
        font-weight: 500;
      }

      header .inner, footer .inner {
        border: solid 1px #D8D8D8;
        border-radius: 2px;
        background-color: #F5F5F5;
      }

      header .inner {
        border-top-style: none;
        border-top-left-radius: 0;
        border-top-right-radius: 0;
      }

      footer .inner {
        border-bottom-style: none;
        border-bottom-left-radius: 0;
        border-bottom-right-radius: 0;
      }

      @media (max-width: 940px) {
        header .inner, footer .inner {
          border-left-width: 0;
          border-right-width: 0;
        }
      }

      .version {
        font-size: 11px;
        position: absolute;
        left: 100%;
        top: 0;
      }

      .docs {
        line-height: 1.2em;
      }

      .docs h1 {
        margin-top: 1.2em;
        font-size: 1.6em;
      }
      .docs h2 { font-size: 1.4em; }
      .docs h3 { font-size: 1.2em; }

      pre code {
        display: block;
        padding: 0.4em 0.6em;
      }

      code {
        background-color: #F9F9F9;
        border: solid 1px #EAEAEA;
        padding: 0em 0.4em;
        border-radius: 2px;
      }

      .readme h1 {
        display: none;
      }

      .app-list {
        list-style: none;
        padding-left: 1.2em;
      }

      .app-list__arrow {
        margin-left: 0.8em;
      }

    </style>
  </head>
  <body>

    <div class="main">
      <header>
        <div class="inner">
          <div class="hero">
            <h1>
              manage-hosts
              <span class="version">{{pkg.version}}</span>
            </h1>
            <div>
              Manage local apps and bind them to some urls
            </div>
          </div>
        </div>
      </header>

      <div class="docs">
        <div class="inner">
          <h1>Applications</h1>
          {{#if apps}}
            <p>Configured apps</p>
            <ul class="app-list">
              {{#apps}}
                <li>
                  <a href="{{href}}">{{href}}</a>
                  <div class="app-list__arrow">&#8594; {{redirect}}</div>
                </li>
              {{/apps}}
            </ul>
          {{/if}}
          {{^apps}}
            <p>
              No apps just yet, go <a href='#usage'>add some apps</a>
            </p>
          {{/apps}}

          <h1>Documentation</h1>
          <div class="readme">
            {{{readme}}}
          </div>
        </div>
      </div>

      <footer>
        <div class="inner">
          Made by a <a href="http://github.com/orangemug">Mug</a>
        </div>
      </footer>
    </div>

  </body>
</html>
